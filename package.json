{
  "name": "android-test-flow-editor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
